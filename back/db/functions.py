from datetime import datetime

def CreateEvent(
        nombre: str,
        descripcion: str,
        lugar: str,
        fecha_inicio: datetime,
        fecha_fin : datetime):
    return "Evento Agendado"